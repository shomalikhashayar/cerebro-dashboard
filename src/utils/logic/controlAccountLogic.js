import { NetworkHelper } from '../../network/NetworkHelper'
import {UrlManager} from '../../network/urls'

class ControlAccountLogic {

    constructor() {
        this.networkHelper = new NetworkHelper();
    }

    // fetch user by id with callback function
    fetchUser(userId, callback) {
        // get account by id
         

        // handle status

        // DTO

        // return
        
    }

    // edit user and make ready for push
    editUser(user,callback){
        // edit user

        // DTO

        // return
    
    }

    // push user to server
    pushUser(user,callback){
        // push user

        // DTO

        // return
    

    }
}

